#ifndef LOGOS_MACROS_H
#define LOGOS_MACROS_H

// LOGOS_METHOD: marks a method as callable by the framework.
// Expands to nothing — scanned by code generators to produce
// callMethod() dispatch and getMethods() metadata.
#define LOGOS_METHOD

#endif // LOGOS_MACROS_H
